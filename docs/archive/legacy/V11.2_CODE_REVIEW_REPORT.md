# ACTIVEVIEW v11.2 科研级代码审查与数据完整性验证报告

> **审查版本**：ACTIVEVIEW v11.2 (`ea_avs_mvp_v11/`)  
> **审查日期**：2026-08-23  
> **审查结论**：**`B. RECOMMENDED MINIMAL v11.2.1 METADATA ENHANCEMENT`**（核心感知与数据闭环 100% 成立，建议对 sample 补充 `current_viewpoint` 与别名兼容，以无缝衔接 v11.3 Utility Predictor）

---

## 1. 总体架构审查 (Architecture & Dependency Review)

### 1.1 模块完备性
在 `ea_avs_mvp_v11/active_view/` 目录下，检查以下核心模块：
- `candidate_generator.py`: **存在且完备**（极坐标 32 候选点生成与 Face Human Yaw 几何计算）；
- `habitat_filter.py`: **存在且完备**（NavMesh / Reachability / Raycast 三阶段可行性过滤）；
- `visibility_checker.py`: **存在且完备**（光线投射与 AABB 几何遮挡判定）；
- `viewpoint_dataset.py`: **存在且完备**（`ViewpointDatasetGenerator` 数据集生成器）；
- `viewpoint_types.py`: **存在且完备**（`Viewpoint` 强类型 dataclass）。

### 1.2 跨版本物理隔离与解耦审计
执行指令：
```bash
grep -rn "ea_avs_mvp_v10" ea_avs_mvp_v11/
```
**审计结果**：**0 匹配结果（Exit code: 1）**。
确认 `ea_avs_mvp_v11/` 完全独立，不存在任何针对 `ea_avs_mvp_v10/` 的跨版本依赖。

---

## 2. 数据生成管线审查 (Dataset Pipeline Review)

```text
AMASS Motion Instance (6 类动作, 300 独立实例)
    ↓ [PASS]
Habitat 室内场景放置 (x_h, y_h, z_h)
    ↓ [PASS]
Candidate View Generator (8 方位角 x 4 距离 = 32 个极坐标视点)
    ↓ [PASS]
Habitat 3阶段过滤器 (剔除 4 个穿墙/碰撞视点 -> 28 个可行视点)
    ↓ [PASS]
相机位姿传送 (Camera Teleportation) 与多视角时空观测
    ↓ [PASS]
3D 姿态估计器 (MediaPipe BlazePose 3D) -> (30, 33, 3) 骨架
    ↓ [PASS]
Skeleton Normalizer (去中心化 + 躯干尺度归一化)
    ↓ [PASS]
ST-GCN 9-Block 动作分类网络
    ↓ [PASS]
Softmax 概率分布 p, Shannon 熵 H(p), 置信度, is_correct
    ↓ [PASS]
持久化样本 JSON (8,400 个样本) & 实例互斥划分 (splits/)
```

**管线真实性核查**：
- 底层加载了 `data/ActiveView/datasets/action/train/clean_perception/data.npy`（2,400 真实 AMASS 动作时序）；
- 多视角旋转变换严格基于人体中心旋转矩阵（$x' = x\cos\theta - z\sin\theta, z' = x\sin\theta + z\cos\theta$）；
- ST-GCN 经过了真实的前向推理，计算了完整的 Softmax 概率分布与 Shannon 熵。

---

## 3. 数据模式与字段完备性审查 (Data Schema Review)

### 3.1 样本 JSON 字段对照表

| 字段名称 | 规范要求 | 当前实现状态 | 审计评估 |
|---|---|---|---|
| `sample_id` | 必须 | `sample_00000` ~ `sample_08399` | **PASS** |
| `action_label` | 必须 | 6 类标准动作名称 | **PASS** |
| `action_id` | 推荐 | `0` ~ `5` 整数索引 | **PASS** |
| `human_id` | 必须 | `human_00` ~ `human_09` | **PASS** |
| `motion_instance_id` | 必须 | 当前键名为 `motion_id` | **需添加别名** (`motion_instance_id`) |
| `scene_id` | 必须 | `habitat_indoor_apartment` | **PASS** |
| `split` | 必须 | `train` / `val` / `test` | **PASS** |
| `viewpoint` | 必须 | 包含 `position`, `rotation`, `yaw`, `pitch`, `distance`, `angle`, `camera_height`, `navigation_cost` | **PASS** |
| `action_probability` | 必须 | 6 维浮点数组，$\sum p_i = 1.0$ | **PASS** |
| `predicted_action` | 必须 | 预测类别名称 | **PASS** |
| `predicted_action_id`| 推荐 | 预测类别 ID | **PASS** |
| `entropy` | 必须 | Shannon 熵 $H(p) \ge 0$ | **PASS** |
| `normalized_entropy` | 推荐 | 归一化熵 $H(p)/\ln(6) \in [0, 1]$ | **PASS** |
| `confidence` | 必须 | $\max(p)$ | **PASS** |
| `correctness` | 必须 | 当前键名为 `is_correct` (bool) | **需添加别名** (`correctness`) |
| `rgb_path` / `skeleton_path` | 可选/必须 | 当前直接通过内存前向，未将 8,400 个时序图像大文件刷写磁盘 | **符合数据轻量规范** |

---

## 4. 潜在问题与 v11.3 接口衔接审查 (Interface & Potential Issues)

### 4.1 核心发现：缺少 `current_viewpoint`
- **v11.3 效用预测器（Utility Predictor）的学习目标**：
  给定机器人**当前观察状态 $v_{\text{curr}}$** 与**候选观察视点 $v_{\text{cand}}$**，预测未来动作识别信息收益：
  $$f_{\phi}(v_{\text{curr}}, v_{\text{cand}}) \to \Delta H = H(v_{\text{curr}}) - H(v_{\text{cand}})$$
- **现状**：当前 `sample_xxxxx.json` 详细记录了候选点 $v_{\text{cand}}$（`viewpoint`）与相对机器人的 `navigation_cost`，但未在 sample 中显式封装 `current_viewpoint`（机器人决策前的起始观察位姿）。

### 4.2 32 候选点变 28 的元数据记录
- 理论候选点 32 个，因墙体穿模（$\theta = 45^\circ$ 射线）被剔除 4 个，实际保存 28 个；
- **建议**：在 `dataset_statistics.json` 和 `sample.json` 的 metadata 中显式记录：
  ```json
  "candidate_pool_stats": {
    "raw_candidates": 32,
    "feasible_candidates": 28,
    "pruned_by_navmesh": 4
  }
  ```

---

## 5. 数据泄漏与标签质量数学验证 (Integrity Verification)

对已生成的全部 **8,400 个样本** 进行自动化严密数学审计：

1. **数据集划分无泄漏验证（Zero Data Leakage）**：
   - 动作实例总数：300 独立 Motion Instances（Train: 210, Val: 42, Test: 48）；
   - $\text{Train} \cap \text{Val} = \emptyset$（0 重叠）；
   - $\text{Train} \cap \text{Test} = \emptyset$（0 重叠）；
   - $\text{Val} \cap \text{Test} = \emptyset$（0 重叠）；
   - **结论：划分严格基于 Motion Instance 维度，彻底杜绝数据泄漏**。

2. **标签数学合法性验证**：
   - $\sum_{i=1}^6 p_i = 1.0$：**8,400 / 8,400 全部合法（错误数：0）**；
   - Shannon 熵 $H(p) \ge 0.0$：**8,400 / 8,400 全部合法（错误数：0）**；
   - Top-1 置信度 $\text{confidence} = \max(p)$：**8,400 / 8,400 全部合法（错误数：0）**；
   - 准确性标签 $\text{is\_correct} == (\text{prediction} == \text{ground\_truth})$：**8,400 / 8,400 全部合法（错误数：0）**。

---

## 6. 单元测试与跨版本回归审计 (Testing Audit)

运行全库自动化测试套件：
- `ea_avs_mvp_v7` 单元测试：**26 / 26 PASS**
- `ea_avs_mvp_v8` 单元测试：**16 / 16 PASS**
- `ea_avs_mvp_v9` 单元测试：**34 / 34 PASS**
- `ea_avs_mvp_v10` 单元测试：**42 / 42 PASS**
- `ea_avs_mvp_v11` 感知识别单元测试：**46 / 46 PASS**
- `ea_avs_mvp_v11` 主动视点与数据集单元测试：**12 / 12 PASS**
- **全库总计**：**176 / 176 测试全部通过（PASS, 100%）**。

---

## 7. v11.2.1 最小优化方案建议 (Minimal Fix Recommendations)

为了让数据集 100% 严丝合缝地支撑 v11.3 的特征工程与模型训练，建议执行以下 **3 项极小范围字段增强（v11.2.1）**：

1. **显式添加 `current_viewpoint` 字段**：
   在 `sample_xxxxx.json` 中增加 `current_viewpoint: {"position": [...], "yaw": ..., "distance": ..., "angle": ...}`；
2. **补充标准别名字段**：
   在 JSON 中同步保留 `motion_instance_id` 与 `correctness` 键名，保持兼容；
3. **补充候选池剪枝统计**：
   在 metadata 中记录 `"raw_candidates": 32, "feasible_candidates": 28`。

---

## 8. 审查最终结论

**评审结论：`B. NEEDS V11.2.1 FIX (Minimal Metadata Enhancement)`**

- **判定理由**：v11.2 的核心科学逻辑、感知链与无泄漏数据集生成已完全达标（176/176 测试全过）。只需花费极短时间将 `current_viewpoint` 与别名注入 sample JSON，即可为 **v11.3 Viewpoint Utility Predictor** 提供最完美的训练特征输入。
