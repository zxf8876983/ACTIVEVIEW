# ACTIVEVIEW v11.0 Engineering Architecture Migration Report

> **报告版本**：ACTIVEVIEW v11.0 Architecture Migration  
> **日期**：2026-08-23  
> **状态**：`MIGRATED, VERIFIED & FULLY SELF-CONTAINED`  
> **核心使命**：重构 ACTIVEVIEW v11.0 工程架构，构建“v11.0 = v10.0 Frozen Baseline + Active View Extension”的独立代码闭环，彻底解除跨版本调用依赖，为后续 v11.2 ~ v11.4 主动视角规划研究奠定工程基础。

---

## 1. 架构重构背景：为什么 v11 需要复制并自包含 v10？

### 1.1 历史问题分析
在早期轻量化探索中，`ea_avs_mvp_v11/` 仅创建了增量的 `active_view/` 目录，需要通过 `from ea_avs_mvp_v10.xxx import yyy` 跨版本调用 3D Pose Estimator、ST-GCN 动作分类器、骨架归一化与不确定度计算模块。

这带来了严重的科研工程隐患：
1. **破坏版本物理隔离**：v11 无法独立部署或运行，产生了隐式的跨版本强耦合；
2. **后续阶段路径混乱**：在 v11.2（视点质量数据集生成）、v11.3（效用预测）与 v11.4（决策策略）中，频繁跨版本调用容易引发命名空间污染与版本回退 Bug；
3. **违反科研版本管理规范**：ACTIVEVIEW 采用大版本独立演进模式，每个大版本均应具备自身完整的生命周期与实验复现能力。

### 1.2 重构核心方案
建立标准的版本演进关系：
$$\mathbf{ea\_avs\_mvp\_v11} = \mathbf{ea\_avs\_mvp\_v10}\ (\text{Frozen Baseline}) + \mathbf{active\_view}\ (\text{Active Perception Extension})$$
- **`ea_avs_mvp_v10/`**：作为被动动作识别与不确定度评价基线（Passive Action Recognition Baseline），**永久保持只读与冻结**；
- **`ea_avs_mvp_v11/`**：复制并集成感知、动作分类、数据集与评价模块，**将内部所有 import 100% 切换为 `ea_avs_mvp_v11.xxx`**，成为具备完整自闭环运行能力的独立版本。

---

## 2. v10 与 v11 的科学关系与定位 (Relationship Matrix)

| 维度 | `ea_avs_mvp_v10` (Frozen Baseline) | `ea_avs_mvp_v11` (Active Perception) |
|---|---|---|
| **核心研究问题** | "How well can the robot understand human actions from an observation?" | "Where should the robot observe next to maximize action recognition reliability?" |
| **状态属性** | 只读基准 (Passive Baseline, Frozen) | 活跃开发版本 (Active View Selection) |
| **感知链路** | MediaPipe BlazePose 3D -> Normalized Skeleton -> ST-GCN | 继承 v10 冻结的感知与分类模型，作为主动决策的观察生成器与评估器 |
| **决策规划能力** | 无（固定视点 / 单次被动观测） | 极坐标候选生成 + Habitat 3阶段过滤 + 效用预测 + 单步移动重观测 |
| **代码依赖关系** | 完全独立，无依赖 | 完全自闭环，**0 跨版本调用** |

---

## 3. 重构后的完整目录结构 (Repository Map)

```text
ACTIVEVIEW/
├── ea_avs_mvp_v10/                     # [FROZEN] 被动动作识别基线 (保持历史只读)
│   ├── perception/
│   ├── action_recognition/
│   ├── dataset/
│   ├── motion/
│   ├── core/
│   └── tests/
│
└── ea_avs_mvp_v11/                     # [ACTIVE] 完整自闭环主动感知版本
    ├── __init__.py
    ├── perception/                     # 3D 姿态估计主链路 (MediaPipe BlazePose 3D)
    │   ├── pose3d_estimator.py
    │   ├── skeleton_normalizer.py
    │   └── skeleton_definition.py
    ├── action_recognition/             # 动作分类网络 (ST-GCN 9-Block) 与不确定度量化
    │   ├── st_gcn_model.py
    │   ├── graph.py
    │   ├── action_classifier.py
    │   └── trainer.py
    ├── dataset/                        # 样本构建与数据集读写
    ├── motion/                         # AMASS 运动管理
    ├── core/                           # 核心路径、类型与配置解析
    ├── evaluation/                     # 骨架评价与对齐工具
    ├── configs/                        # 统一配置文件
    │   ├── default_v11_config.yaml
    │   └── viewpoint_config.yaml       # 主动视点采样与过滤配置
    ├── active_view/                    # 【v11 核心新增】主动视角选择模块
    │   ├── __init__.py
    │   ├── viewpoint_types.py          # Viewpoint 强类型 dataclass
    │   ├── candidate_generator.py      # 人体中心极坐标 32 候选点生成 (Face Human Yaw)
    │   ├── visibility_checker.py       # 视线光线投射 (Raycast) 通视性检测
    │   ├── habitat_filter.py           # NavMesh 可行、Path 可达与 Raycast 可见三阶段过滤
    │   ├── viewpoint_dataset.py        # [v11.2 接口 Stub] 视点质量数据集生成
    │   ├── utility_predictor.py        # [v11.3 接口 Stub] 视点效用预测器
    │   ├── selector.py                 # [v11.4 接口 Stub] 单步决策策略
    │   └── tests/                      # active_view 专项单元测试
    ├── tools/
    │   └── visualization/
    │       └── viewpoint_visualizer.py # 2D 鸟瞰图与极坐标雷达图可视化工具
    ├── scripts/
    │   └── run_v11.py                  # v11 端到端综合运行验证入口
    └── tests/                          # v11 完整单元测试套件 (50 项测试)

├── run_v11.py                          # 根目录统一快捷入口 -> ea_avs_mvp_v11/scripts/run_v11.py
└── configs/viewpoint_config.yaml       # 根目录配置文件别名
```

---

## 4. 新增 active_view 模块与阶段接口规范

### 4.1 已完成实现模块 (v11.1)
1. **`active_view/viewpoint_types.py`**：定义 `Viewpoint` dataclass（包含三维坐标、方位角、视距、朝向角 `yaw`、俯仰角 `pitch`、`navigation_cost`、`is_navigable`、`is_reachable`、`is_visible`、`is_feasible` 等完整字段）；
2. **`active_view/candidate_generator.py`**：基于人体中心极坐标系采样生成 32 个候选视点（8 方位角 $\times$ 4 视距），并自动计算注视人体的偏航角（$\text{yaw} = \text{atan2}(\Delta x, \Delta z)$）；
3. **`active_view/visibility_checker.py`**：基于物理光线投射（Habitat `sim.cast_ray`）与几何 AABB 检测机器人相机与人体各关键部位的视线通视性；
4. **`active_view/habitat_filter.py`**：三阶段严格过滤候选空间，输出真实具备可行性的视点集合与结构化统计日志。

### 4.2 后续子版本标准开发接口 (Stub)
为保障 v11.2 ~ v11.4 的平滑演进，已预置标准接口类：
1. **v11.2 视点质量数据集接口 (`viewpoint_dataset.py`)**：
   ```python
   class ViewpointDatasetGenerator:
       def generate_viewpoint_dataset(self, output_dir, motion_ids, samples_per_action):
           ...
   ```
2. **v11.3 效用预测器接口 (`utility_predictor.py`)**：
   ```python
   class ViewpointUtilityPredictor:
       def predict_utility(self, current_observation, candidate_viewpoints):
           ...
   ```
3. **v11.4 主动决策选择策略接口 (`selector.py`)**：
   ```python
   class ActiveViewSelector:
       def select_best_viewpoint(self, candidate_viewpoints, utility_scores):
           # Score(v) = Gain(v) - lambda * Cost(v)
           ...
   ```

---

## 5. 统一端到端运行入口验证 (`run_v11.py`)

运行命令：
```bash
python run_v11.py --action walking
```

### 实际执行日志输出：
```text
=================================================================
  ACTIVEVIEW v11.0: Autonomous Active Perception Pipeline Demo  
=================================================================
Step 1: Initializing ea_avs_mvp_v11.perception.MediaPipe3DPoseEstimator...
Step 2: Initializing ea_avs_mvp_v11.action_recognition.ActionClassifier...
Loaded ST-GCN checkpoint from: /home/zxf/WorkSpace/code/data/ActiveView/checkpoints/v10_st_gcn/best_st_gcn_model.pth
Step 3: Processing RGB video observation for action: 'walking'...
  Extracted 3D Skeleton Sequence: shape=(30, 33, 3), mean_confidence=0.850
Step 4: Normalizing skeleton and evaluating ST-GCN Action Classifier...
  Current Recognition Result:
    Predicted Action:    walking (True: walking)
    Top-1 Confidence:    0.9982
    Shannon Entropy H(p):0.0124 (Normalized Uncertainty: 0.0069)
Step 5: Generating candidate viewpoints around human at [0.0, 0.0, 0.0]...
Candidate Generation: Generated 32 candidate viewpoints (Angles: [0.0, 45.0, 90.0, 135.0, 180.0, 225.0, 270.0, 315.0], Distances: [1.5, 2.0, 2.5, 3.0])
Step 6: Applying Habitat 3-stage feasibility filtering...
=========================================================
Candidate Generation:
  Generated candidates: 32

Filtering:
  Navigable: 29 / 32
  Reachable: 29 / 29
  Visible:   28 / 29

Final feasible viewpoints: 28 / 32
=========================================================
=================================================================
  Pipeline Completed Successfully! Feasible Candidates: 28 / 32  
=================================================================
```

---

## 6. 测试与解耦审计结论

1. **依赖解耦审计（Dependency Audit）**：
   - 运行 `grep -rn "ea_avs_mvp_v10" ea_avs_mvp_v11/` $\to$ **0 匹配（完全解耦）**；
2. **`ea_avs_mvp_v11` 内部测试（50 项测试）**：
   - `tests/unit/` (42 项感知与动作分类测试): **42 / 42 PASS (100%)**；
   - `active_view/tests/` (8 项主动视点与过滤测试): **8 / 8 PASS (100%)**；
3. **全库历史跨版本回归测试（168 项测试）**：
   - v7 (26), v8 (16), v9 (34), v10 (42), v11-unit (42), v11-active_view (8) $\to$ **168 / 168 PASS (100%)**。

---

## 7. 结论与后续开发准则
- `ea_avs_mvp_v10` 与 `ea_avs_mvp_v11` 已建立清晰的物理与代码隔离边界；
- **后续所有 v11.2 ~ v11.4 的开发与实验，均直接且仅在 `ea_avs_mvp_v11/` 目录下完成**。
