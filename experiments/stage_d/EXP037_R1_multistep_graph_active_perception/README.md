# EXP037-R1 — Context-safe multistep graph active perception

The frozen H=1/2/3 graph protocol is evaluated after the context identity
repair.  Terminal HAR Accuracy and Macro-F1 use the selected frozen ST-GCN
observation; oracle rows are explicitly privileged diagnostics.
