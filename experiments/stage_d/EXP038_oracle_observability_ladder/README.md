# EXP038 — Privileged Oracle Observability Ladder

Train/Val-only diagnostic. L0 is legal current state plus candidate geometry;
L1 adds the privileged GT activity label; GT motion extraction is explicitly
blocked because the repository exposes no canonical GT-to-joint mapping.
ClassViewPrior is a low-capacity privileged label-conditioned baseline.

No Test, perception regeneration, Habitat rendering or ST-GCN retraining was
performed. Privileged rows are observability upper bounds, not deployable
policies.
