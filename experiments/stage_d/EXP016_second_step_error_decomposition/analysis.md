# EXP016 analysis record

## Observation

Real Val analysis is not yet executed.  The implementation is prepared to
produce a common trajectory-metric table for EXP014, OracleGate +
LearnedCandidate, LearnedGate + OracleCandidate and the Fixed-first
Second-Step Oracle, together with gate/candidate recovery fractions and
denominator-labelled candidate-hit diagnostics.

## Interpretation

No scientific interpretation is authorized before the real Val-only analysis
is explicitly reviewed and run.  The code keeps the first Stage C-v0 action
fixed and separates gate control from candidate identity, so any future
comparison will address the registered EXP016 question without changing the
frozen upstream protocol.

## Decision

**INCONCLUSIVE (preparation only).** No policy is accepted or rejected by this
preparation commit.

## Next

Human code review and explicit authorization are required before executing the
EXP016 Val command.  Do not read Test or start a follow-up experiment
automatically.

`test_used=false`; `training_performed=false`.
