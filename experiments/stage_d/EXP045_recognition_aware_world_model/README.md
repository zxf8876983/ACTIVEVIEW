# EXP045 — Recognition-Aware World Model

Variants E/F of the EXP042-R1 dense world-model rerun. The frozen ST-GCN is
used only as a Train/Val recognition-consistency teacher; it is never updated.
Recognition loss weight is fixed at 0.10. Test remains locked.
