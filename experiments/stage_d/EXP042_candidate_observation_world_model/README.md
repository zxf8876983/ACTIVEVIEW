# EXP042 — Candidate Perceived-Skeleton World Model

Candidate-conditioned world models (WM-A skeleton, WM-B + frozen recognition
belief, WM-C + visited spatial RGB) are trained on Train targets and evaluated
on Val.  Checkpoints remain outside Git; compact summaries are committed.
