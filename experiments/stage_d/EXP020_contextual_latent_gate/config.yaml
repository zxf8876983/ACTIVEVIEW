experiment_id: EXP020
name: contextual_latent_executed_gate
split:
  train: train
  evaluation: val
  test_used: false
model:
  frozen_exp014_contextual_token_dim: 128
  predicted_utility_dim: 1
  input_dim: 129
  hidden_dim: 64
  activation: GELU
  loss: BCEWithLogitsLoss
training:
  seed: 42
  epochs: 30
  batch_size: 256
  optimizer: Adam
  learning_rate: 0.001
  selection: final_epoch
decision:
  threshold_probability: 0.5
  strict_greater_than: true
protocol:
  candidate_ranking: frozen_exp014_predicted_utility_geodesic_viewpoint_id
  first_step: frozen_stage_c_v0
  true_u2_as_model_input: false
  exp014_retrained: false
