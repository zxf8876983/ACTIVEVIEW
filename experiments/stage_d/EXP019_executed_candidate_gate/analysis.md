# EXP019 analysis

The fixed Train→Val experiment is executed by `run.sh`. This file will record
the final Train diagnostics, Val trajectory comparison and the controlled
interpretation after the authorized run.

The experiment is not a deployable-policy acceptance. Test remains locked and
EXP020 must not be started automatically.
