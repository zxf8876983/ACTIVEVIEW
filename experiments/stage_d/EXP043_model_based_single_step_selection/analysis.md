# EXP043

Selector code is prepared for imagined future skeletons scored by frozen
ST-GCN.  The GT-label selector is explicitly privileged/non-deployable and
legal selectors use only predicted observations.  It remains blocked until a
real EXP042 checkpoint exists.  Test was not read.
