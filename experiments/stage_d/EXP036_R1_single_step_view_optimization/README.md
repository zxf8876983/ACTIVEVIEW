# EXP036-R1 — Context-safe single-step view optimization

Dense regression, Bradley–Terry, graph smoothing and Bayesian diagnostics are
rerun with one independent dense field per `(scene_id, region, record_id)`.
Candidate-winner, oracle-action and final HAR metrics are reported separately.
