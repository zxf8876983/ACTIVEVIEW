experiment:
  id:
  name:
  stage: stage_c_v1

baseline:
  model:
  checkpoint:

change:
  category:
  description:

frozen:
  stage_a: true
  stage_b: true
  stage_c_features: true
  stgcn_checkpoint: true
  record_split: true
  candidate_protocol: true

training:
  seed:
  epochs:
  batch_size:
  learning_rate:

evaluation:
  train: true
  val: true
  test: false

protocol:
  test_locked: true
  test_authorized: false
