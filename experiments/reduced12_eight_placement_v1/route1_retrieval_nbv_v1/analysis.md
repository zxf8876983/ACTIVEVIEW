# Route-1 Retrieval NBV

The selector uses only current frozen feature/posterior, viewpoint identity and candidate geometry. Future candidate observations and labels are not read before action selection.
