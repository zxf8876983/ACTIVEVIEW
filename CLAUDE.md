# ACTIVEVIEW

Read first:

1. `AGENTS.md`
2. `.ai/PROJECT_STATE.md`
3. `.ai/CURRENT_TASK.md`

Use `activeview/` as the only source package. Do not scan
`docs/archive/` unless historical context is explicitly needed.

Follow the scientific invariants in `AGENTS.md`. For an experiment, read its
`experiments/.../README.md` and `config.yaml`. Do not introduce experiment-
management infrastructure unless explicitly requested.
