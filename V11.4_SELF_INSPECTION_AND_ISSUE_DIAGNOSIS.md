# ACTIVEVIEW v11.4 实验问题深度自查与整改方案文档

> **文档版本**：v11.4-SelfInspection-v1.0  
> **审查对象**：ACTIVEVIEW v11.4 闭环主动感知实验、动作识别分类器与数据集生成流程  
> **核心宗旨**：直面科研实验中的作弊/简化问题，严密排查准确率虚高（100%）的深层技术原因，明确界定训练与测试感知边界，制定彻底的 Habitat 真实渲染与姿态估计器提取整改方案。

---

## 1. 核心问题自查与根因深度剖析

### 1.1 问题一：测试评估中直通使用真值 3D 骨架，跳过 Habitat 真实传感器感知与 Pose 估计器提取

- **表象现象**：
  在 v11.4 的 100 轮闭环基准测试中，无论是停留在沙发盲区的 `Nearest`（贪心最近视点）、随机盲选的 `Random`，还是规划绕障的 `Ours`，其移动后动作识别准确率（`Acc_after`）**全部显示为 100.00%**。
- **作弊/简化根因**：
  1. **真值骨架直通（Ground-Truth Bypass）**：
     在 `ea_avs_mvp_v11/active_view/closed_loop_episode.py` 的 `_evaluate_viewpoint_perception` 中，代码直接调用了 `self._get_skeleton(action_id, instance_idx)` 获取 AMASS 的无损三维骨架真值，并仅对其施加数学刚体旋转 $P_{\text{rotated}} = R_y(\theta) \cdot P_{\text{GT}}$；
  2. **跳过 Habitat RGB-D 传感器渲染与姿态检测**：
     系统**完全没有在 Habitat-Sim 中将仿真人体放入住宅网格并由机器人相机拍摄真实的 RGB-D 图像**，也**完全没有调用 2D/3D Pose 估计器（如 MediaPipe / Keypoint Detector + Depth Lifting）从图像中提取估计骨架**；
  3. **温度缩放无法改变 $\text{argmax}$ 类别**：
     代码虽然通过分析公式计算了物理遮挡率并对分类概率进行了温度缩放（$\text{scaled\_logits} = \text{logits} / T$），成功压扁了置信度（从 99% 降至 56%）并抬高了熵，**但温度缩放属于单调线性变换，永远无法改变 $\text{argmax}(\text{logits})$ 的最大值类别**。送入分类器的骨架是 100% 完好无损的真值，因此分类器永远输出正确类别。

```
                     【当前存在作弊/简化的感知链路】
  AMASS 真值 3D 骨架 ──> 仅做数学刚体旋转 ──> Canonical 对齐 ──> ST-GCN (100% 无损输入)
  (完全未经过 Habitat 渲染，未经过家具遮挡碰撞，未经过 Pose 估计器，准确率虚高全为 100%)
```

---

### 1.2 问题二：训练阶段与测试评估阶段的感知边界与数据规范未严格隔离

- **科学实验协议要求**：
  - **从头到尾严禁使用 AMASS Ground-Truth 3D 骨架直接作为输入**；
  - **训练阶段 (Train Phase - Clean Perception)**：
    必须在**纯净背景（无复杂家具遮挡）**下渲染仿真人体 $\to$ 运行 Pose 估计器从 RGB-D 图像提取估计骨架 $\to$ 送入 ST-GCN 进行分类训练；
  - **测试评估阶段 (Test Phase - Habitat Environment Perception)**：
    必须将仿真人体放入 **Habitat 真实家庭住宅场景（沙发、餐桌、立柜之间）** $\to$ 机器人在各候选视点拍摄真实的 RGB-D 观测图像 $\to$ 运行 Pose 估计器进行关键点检测与深度投影（受到真实家具物理遮挡，不可见的下肢/躯干产生真实缺失与检测噪声） $\to$ 将提取的残缺/估计骨架送入 ST-GCN 进行动作识别。
- **现状偏差**：
  - 训练集直接读取了理想坐标，测试集直接复用了理想坐标；
  - 缺少从“纯净估计骨架训练”到“复杂环境残缺骨架测试”的真实 Domain Gap 与物理遮挡因果链。

---

### 1.3 问题三：机器人与人体放置的空间遮挡物理交互不充分

- **现状分析**：
  在 `robot_start_sampler.py` 中虽然初始机器人的方位角是在 $[0^\circ, 360^\circ]$ 采样，但在后续候选点感知推断时，由于缺乏真实 Habitat 场景深度射线阻断（Raycast Collision），视线无法被沙发布料或木质桌腿真正截断，导致机器人即使处于人体后方或障碍物后方，依然获得了“透视眼”。

---

## 2. 真实物理环境下的主动感知预期表现

当完全接入 Habitat 真实 RGB-D 渲染与 Pose 估计器提取流水线后：

```
                 【严格的 Habitat 物理闭环主动感知真实链路】
  ┌────────────────────────────────────────────────────────────┐
  │  1. 真实 Habitat 场景网格放置 (沙发后 / 餐桌旁)             │
  └────────────────────────────────────────────────────────────┘
                               │
                               ▼
  ┌────────────────────────────────────────────────────────────┐
  │  2. 机器人视点渲染真实 RGB-D 传感器观测 (受真实家具网格遮挡) │
  └────────────────────────────────────────────────────────────┘
                               │
                               ▼
  ┌────────────────────────────────────────────────────────────┐
  │  3. Pose 估计器从图像中提取估计骨架 P_estimated           │
  │     - 遮挡视点 (如 Nearest 停在沙发后): 下肢关键点无法检出 │
  │     - 良好视点 (如 Ours 绕障到正面): 完整检出 33 关节点    │
  └────────────────────────────────────────────────────────────┘
                               │
                               ▼
  ┌────────────────────────────────────────────────────────────┐
  │  4. 送入 ST-GCN 进行动作识别与不确定度计算                 │
  │     - 不良视点 (Nearest / Random): 准确率暴跌至 30% ~ 55%  │
  │     - 主动视点 (Utility Predictor / Ours): 准确率达 90%+   │
  └────────────────────────────────────────────────────────────┘
```

- **不良视点（如 `Nearest` 贪心停留在家具盲区）**：
  - 机器人拍摄的 RGB-D 图像中，人体的下半身被沙发遮挡；
  - **Pose 估计器（MediaPipe / ViTPose）无法在图像中检测到被挡住的髋部、膝盖和脚踝**，深度图也被家具表面深度截断；
  - 输出的估计骨架带有**真实的关键点丢失与检测误差**；
  - ST-GCN 面对残缺骨架极易发生混淆（例如将遮挡下肢的 `sit_down` 误判为 `squatting` 或 `bending`），**准确率将真实下降至 $30\% \sim 55\%$**！
- **主动最优视点（`Utility Predictor / Ours`）**：
  - 机器人规划移动至无遮挡的最佳感知视点，完整拍摄人体全身；
  - Pose 估计器完整提取出 33 个关节点；
  - **ST-GCN 恢复 $90\% \sim 95\%$ 的高准确率与低后验熵！**
- **科研结论的真正成立**：
  只有在此时，主动视角的“信息增益（$\Delta H$）”才能与“识别准确率（$\text{Acc}$）”产生严密的科学正相关，真正证明主动感知在复杂环境下的巨大实用价值。

---

## 3. 彻底整改方案与技术实施路线图

### 3.1 改造一：实现端到端 Habitat RGB-D 渲染与 Pose 提取闭环 (`HabitatPosePerceptionPipeline`)
1. **废除任何真值直通代码**：
   - 彻底删除 `closed_loop_episode.py` 中直接对 AMASS 3D 真值进行数学旋转作为感知的逻辑；
2. **对接 Habitat 图像捕获与 Pose 估计器**：
   - 统一调用 `RGBDSkeletonExtractor`（基于 MediaPipe 2D 检测 + Habitat 深度图局部中值反向投影）；
   - 机器人到达任何视点 $(X, Y, Z, \text{yaw})$ 时，必须由 Habitat-Sim 渲染真实传感器图像 `(rgb, depth)`；
   - 严格由 Pose 估计器对图像进行处理，输出带有真实可见性掩码与坐标噪声的 `EstimatedSkeleton3D`。

### 3.2 改造二：规范训练集与测试集的生成协议
1. **训练集（Clean Perception）**：
   - 在纯净无遮挡虚拟摄影棚环境渲染 3D 仿真人体；
   - 由 Pose 估计器提取纯净估计骨架，训练 16 类 ST-GCN；
2. **测试集（Habitat Multi-Scene Perception）**：
   - 将人体放置在 10 套 Habitat 住宅场景各具挑战的家具位置（沙发后、餐桌旁、床边）；
   - 在机器人视点由 Pose 估计器提取受环境影响的骨架，执行动作识别测试。

### 3.3 改造三：重新执行 100 轮闭环基准测试与报告重写
- 在真实的感知残缺与误差干扰下，重新运行 100 轮闭环评测；
- 真实记录各策略的 `Acc_after`、`Entropy`、`Confidence` 与 `Navigation Efficiency`；
- 彻底拉开 `Ours` 与 `Nearest` / `Random` 的性能差距，形成严谨扎实的科研成果。

---

## 4. 总结与承诺

ACTIVEVIEW 团队坚决恪守科研诚实与物理真实性原则：
1. **承认问题**：v11.4 快速原型中存在使用真值 3D 骨架直通导致的准确率虚高（100%）硬伤；
2. **彻底整改**：封死真值直通，全面接入 Habitat 真实传感器渲染与 Pose 估计器感知链路；
3. **真实产出**：以经受住真实环境遮挡检验的科学数据，全面呈现主动视角选择算法的核心贡献。
